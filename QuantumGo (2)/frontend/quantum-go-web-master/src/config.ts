// const Config = {
//   apiUrl: `${window.location.protocol}//${window.location.host}/api`,
//   wsUrl: `ws://${window.location.host}/ws`
// };
const host = "34.123.184.184";
const Config = {
  apiUrl: `http://${host}:3000`,
  wsUrl: `ws://${host}:3000/ws`
};

export default Config;
